[build]
  command = "pnpm build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  PNPM_FLAGS = "--no-frozen-lockfile"
  SECRETS_SCAN_OMIT_KEYS = "COGNO_USER"
  SECRETS_SCAN_OMIT_PATHS = "README.md,wear_next/README.md"

